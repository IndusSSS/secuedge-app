plugins {
    id("com.android.library")
    id("org.jetbrains.kotlin.android")
}

android {
    namespace = "com.smartsecurity.secuedge.core"
    compileSdk = 35
    defaultConfig { minSdk = 29 }
}

dependencies {
    // TODO: core module dependencies
}
