package com.secuedge.domain

/**
 * Syncs local logs and media to the cloud.
 */
class SyncCloudUC {
    suspend operator fun invoke(): EdgeResult {
        // TODO implement cloud sync
        return EdgeResult.Success
    }
}
